<script>
  function validate_buyorder(){
    //$(".error").remove();
      var valid=true;
/*
      if($("#customer-form-code").val()=="" && $("#salesorder-form-number").val()=="")
      {

       toastr.error("Por favor, selecciona un cliente o un pedido", "Confirmación", confirmation);
        valid=false;
        return valid;
      }

      if($("#salesticket-form-observations").val()=="")
      {
        toastr.error("Por favor, añade una observación a la incidencia", "Confirmación", confirmation);
         valid=false;
         return valid;

      }
      if($("#salesticket-form-states-select").val()=="-1")
      {
        toastr.error("Por favor, selecciona un estado", "Confirmación", confirmation);
         valid=false;
         return valid;

      }

*/
  return valid;

}
</script>
