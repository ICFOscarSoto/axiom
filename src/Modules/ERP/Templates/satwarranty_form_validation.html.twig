<script>
  function validate_satwarranty(){
    $  (".error").remove();
      var valid=true;

      if($("#customer-form-code").val()=="")
      {

       toastr.error("Por favor, selecciona un cliente", "Confirmación", confirmation);
        valid=false;
        return valid;
      }

      if($("#product-form-code").val()=="")
      {
       toastr.error("Por favor, selecciona un producto", "Confirmación", confirmation);
        valid=false;
        return valid;
      }

      if($("#satwarranty-form-description").val()=="")
      {
        toastr.error("Por favor, añade una descripción", "Confirmación", confirmation);
         valid=false;
         return valid;

      }
      if($("#satwarranty-form-currentstate").val()=="-1")
      {
        toastr.error("Por favor, selecciona un estado", "Confirmación", confirmation);
         valid=false;
         return valid;

      }


      /*
      if($(this).find("#product-form-suppliers-"+$(this).attr("attr-id")).val()=="-1")
      {
       code=$(this).find("#product-form-code-"+$(this).attr("attr-id")).val()
       toastr.error("No se ha seleccionado proveedor para el producto "+code, "Confirmación", confirmation);
        valid=false;
        return valid;
      }
*/

  return valid;

}
</script>
